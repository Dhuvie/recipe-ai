# Fridge to Recipe Application

## Overview

The Fridge to Recipe Application is a full-stack web architecture designed to dynamically generate structured culinary recipes based on user-provided ingredients. It utilizes a React and Vite frontend coupled with a lightweight Express backend proxy. The backend securely interfaces with the Google Gemini AI API to stream generated content via Server-Sent Events (SSE). The frontend consumes this stream to render interactive components, including recipe cards, ingredient checklists, and nutritional macronutrient charts, in real-time.

## System Requirements and Setup

### Prerequisites
- Node.js environment
- Google Gemini API Key

### Backend Configuration
1. Navigate to the `server` directory:
   ```bash
   cd server
   ```
2. Install necessary dependencies:
   ```bash
   npm install
   ```
3. Configure environment variables by copying `.env.example` to `.env` and supplying your API key:
   ```env
   GEMINI_API_KEY=your_actual_key_here
   PORT=3001
   ```
4. Initialize the backend server:
   ```bash
   node index.js
   ```

### Frontend Configuration
1. In a separate terminal session, navigate to the `client` directory:
   ```bash
   cd client
   ```
2. Install necessary dependencies:
   ```bash
   npm install
   ```
3. Initialize the development server:
   ```bash
   npm run dev
   ```

## Usage

Once both servers are actively running, navigate to `http://localhost:5173` in a web browser. Enter available ingredients into the primary input field. The application will construct a prompt, securely query the AI service through the backend proxy, and systematically stream the structured recipe response into the user interface. Previous queries can be accessed via the session history sidebar. The application additionally respects system-level theme preferences for dark and light modes.

## Known Limitations

- The API request timeout is statically defined at 20 seconds on the backend and 25 seconds on the frontend.
- While the user interface includes error handling, it does not currently implement automatic request retries upon failure.
- Session history is maintained in local browser storage and lacks persistent cross-device or server-side synchronization.

## AI Usage Note

Antigravity, an advanced agentic AI coding assistant developed by Google Deepmind, was utilized extensively during the development of this application. Antigravity was responsible for generating the boilerplate configurations for Vite, establishing the React component structures, implementing the CSS styling via Tailwind and Shadcn UI, and scaffolding the Express backend proxy. Additionally, Antigravity assisted in writing the Zod data validation schemas, setting up the Server-Sent Events (SSE) logic to interface with the Google Gemini API, and dynamically debugging runtime errors and configuration issues. All code generated by Antigravity has been thoroughly reviewed and can be comprehensively explained by the developer.

## Time Spent

The total time allocated to the development of this application is approximately 3 hours.
